#!/usr/bin/env bash

set -euo pipefail

signal="${1:-9}"
pids=$(ps -A -o pid= -o comm= | fzf --multi --delimiter=' ' --nth=2.. | awk '{print $1}')

if [ -n "$pids" ]; then
	printf '%s\n' "$pids" | xargs sudo kill -"$signal"
fi
